# 🐢 龟龟水印 · 从零到能用的全流程说明

一份写给自己（也写给对代码不了解的朋友）的项目说明。回答几个核心问题：这个东西是用什么做的，为什么打开网页就能用，为什么手机和电脑长得不一样，Git 和 GitHub 到底扮演什么角色。

---

## 一、这个软件是什么

一个跑在浏览器里的图片工具，两个核心功能：

1. **去水印**：用画笔涂掉图片上的水印/logo/文字，AI 把涂掉的区域"猜"回来
2. **加水印**：给自己的商品图/素材图加文字水印，支持多水印、模板、批量处理
3. **前后对比**：一条可拖动的竖线，左侧显示修改后、右侧显示原图，随手对比效果

电脑版和手机版是**同一套代码库里的两个页面**，各自适配自己的屏幕尺寸和交互习惯。

---

## 二、用了哪些编程语言

**只用了三种，都是网页原生的**：

| 语言 | 干什么 | 在项目里对应哪些文件 |
|---|---|---|
| **HTML** | 描述"页面上有什么" —— 按钮、画布、文字输入框 | `ImageWR.html`（电脑版）、`mobile.html`（手机版） |
| **CSS** | 描述"这些东西长什么样" —— 颜色、布局、深色模式、圆角 | 写在 `<style>` 标签里，和 HTML 在同一个文件 |
| **JavaScript** | 描述"用户点了会发生什么" —— 全部业务逻辑 | 写在 `<script>` 标签里，另外还有 `ai-inpaint.js`、`sw.js` |

关键决定：**没有用任何前端框架**（不用 React/Vue/Angular），也**没有编译步骤**（不用 Webpack/Vite）。这样有几个好处：

- 一个 HTML 文件双击就能跑，不需要 `npm install`
- 分享给别人只需要把仓库压缩传过去
- 没有中间层，浏览器读到什么就是什么，调试简单

---

## 三、用了哪些工具

### 编辑器 / 开发环境
- **VS Code**：写代码
- **Claude Code**（就是我，AI 编程助手）：负责实际写代码、改 bug、组织架构
- **浏览器开发者工具**（F12）：调试运行时问题、看错误信息

### 浏览器原生的 API（不是外部依赖）
这些是浏览器本来就自带的能力，直接调用：

- **Canvas API**：图片处理的核心。所有的画笔、水印渲染、AI 推理输入/输出都在 Canvas 上做
- **File API**：用户"打开图片"、"批量选图" 走这个
- **Pointer Events**：统一的鼠标/触摸事件，代码里一份写法既能响应鼠标也能响应手指
- **localStorage**：存"我的水印库"和"我的模板"，浏览器本地存储，永不上传
- **Service Worker**：网页版的"后台守护进程"，负责缓存文件让你离线也能用
- **Web App Manifest**：告诉浏览器"我这个网页可以装成桌面/主屏图标"

### 唯一的第三方库
- **onnxruntime-web**（`vendor/ort/` 目录下）：微软开源的浏览器端 AI 推理引擎。让 AI 模型能在你自己的手机上跑，不需要传图到服务器。
  - `ort.min.js`：JavaScript 胶水代码
  - `ort-wasm-simd-threaded.wasm`：真正干活的 WebAssembly 二进制（11 MB）
  - `ort-wasm-simd-threaded.mjs`：wasm 加载器

### AI 模型
- **MI-GAN inpainting**（`migan_pipeline_v2.onnx`，28 MB）：一个专门做"图像补全"的神经网络。你涂哪儿它就把哪儿"想"回来。ONNX 是一种跨框架的模型格式，可以直接在浏览器里跑。

---

## 四、为什么打开网页就能用（不用装软件）

### 传统软件怎么运作
"下载 → 安装 → 运行" 三步。软件是一堆二进制文件，操作系统直接执行。

### 网页应用怎么运作
浏览器就是一个"通用运行时"。你访问一个网址，浏览器：

1. 从服务器下载 HTML/CSS/JS/图片
2. 立刻在你的机器上解析并执行这些代码
3. 关闭页面 → 一切消失（除非用了 localStorage 或 Service Worker 缓存）

### 我们做的额外一步：PWA（渐进式 Web 应用）
普通网页每次访问都要联网。**PWA** 让网页升级到"和 App 差不多"的等级：

- `sw.js`（Service Worker）在你**第一次访问后**，把所有文件（HTML、JS、AI 模型、字体、图标）都缓存到你的设备本地
- 第二次访问：即使断网也能用，所有资源从本地缓存加载
- `manifest.webmanifest` / `app.webmanifest` 告诉浏览器"我可以被安装"，你会看到一个 "安装到桌面"/"添加到主屏幕" 的按钮
- 装完之后长得跟原生 App 一样，独立窗口，有图标，可以离线用

**为什么图片不会被上传？**
因为**所有 JavaScript 代码都在你自己的浏览器里执行**。图片是通过 Canvas API 加载到你自己的内存里处理的，AI 模型也是在你的 CPU 上算的。整个过程没有一句代码写了"把图片发到服务器"，所以物理上就上传不了。

---

## 五、为什么手机版和电脑版长得不一样

**因为屏幕不一样、操作习惯不一样、性能不一样**。

强行把电脑界面塞进手机会体验很差（按钮太小、侧边栏挤爆、双指缩放和涂抹冲突），所以我们做了两套 HTML：

| | 电脑版 `ImageWR.html` | 手机版 `mobile.html` |
|---|---|---|
| 布局 | 左侧 260px 侧边栏 + 主画布 | 顶栏 + 全屏画布 + 底部工具栏 |
| 详细设置 | 直接展开在侧边栏 | 从底部滑出 sheet（弹出面板） |
| 涂抹 | 鼠标拖动 | 单指拖动 |
| 缩放 | Ctrl+滚轮 | 双指捏合 |
| 分辨率上限 | 原图分辨率（可选） | 最大 2400px（手机内存小） |
| AI 推理 | 更清晰选项开放到原图 | 默认到 1024px（防 OOM） |
| 保存图片 | 直接下载 PNG | 优先调用系统分享面板（可存到相册） |

**关键点**：**核心逻辑（Canvas 处理、AI 推理、水印渲染、模板管理）两个版本共用**，只是外层 UI 布局不同。这就是为什么改一个功能要同步改两个文件。

---

## 六、Git 和 GitHub 到底是什么

### Git（本地工具）
Git 是一个**给文件夹拍快照的工具**。你在项目里改代码，改错了想撤销，改对了想记录进度 —— Git 记账。

一次"提交"（commit）= "现在这个瞬间整个项目长什么样"。每个提交都有个哈希 ID（比如 `ec0ffcf`），可以随时跳回去。

**打个比方**：
- 项目文件 = 一本正在写的书
- Git = 每写完一章就存一个存档
- 提交历史 = 你能一路翻到"第一章刚开始"的样子

### GitHub（云端服务）
GitHub 是一个把 Git 快照**放到网上供别人下载/协作**的网站。

**打个比方**：
- Git 是本地写日记
- GitHub 是发博客

`git push`：把本地的快照送到 GitHub 服务器
`git pull`：把 GitHub 上的最新快照拉到本地
`git clone`：把 GitHub 上的整个项目复制到本地一份

### 项目里那些 Git 操作都在干什么

| 命令 | 干什么 | 什么时候用 |
|---|---|---|
| `git status` | 看当前有哪些文件改动过但还没记录 | 每次开工前、提交前 |
| `git add 文件名` | 告诉 Git："这些改动我要记录下来" | 想提交某几个文件时 |
| `git commit -m "说明"` | 给这批改动拍个快照，附一句话描述 | 完成一个小功能/修完一个 bug |
| `git push` | 把本地快照送到 GitHub | 想让别人（或未来的自己）能看到 |
| `git log` | 看历史提交列表 | 想回顾都做了什么 |
| `.gitignore` | 一个文件，列出"不要记录的东西"（临时文件、密码等） | 项目里 |

---

## 七、为什么发布到 GitHub 上，别人打开就能用

因为 GitHub 提供了一个免费服务叫 **GitHub Pages**。

### 常规网站怎么发布
1. 买域名
2. 租服务器
3. 把 HTML/JS 文件传到服务器
4. 配置 nginx/Apache 之类的 Web Server
5. 让服务器对着一个公网 IP 提供服务
6. 用户访问 `你的域名/mobile.html` 就能看到

### GitHub Pages 做的
1. **你 push 到 GitHub** → 就等于把文件传到了服务器
2. **GitHub 自带 Web Server** → 帮你把仓库里的 HTML 挂到网上
3. **GitHub 给你分配一个二级域名** → `你的用户名.github.io/仓库名/文件名.html`
4. 免费、免维护、有 CDN 加速、支持 HTTPS

所以对我们来说，发布就是一句 `git push`。远端的仓库地址是 `github.com/Kimoji798/dewatermark`，对应的公开访问地址就是 `kimoji798.github.io/dewatermark/mobile.html`。

**任何知道链接的人**，用浏览器打开都能立刻用，不需要注册、不需要下载、不需要装环境。

### 更进一步：把网页装成 App
用户第一次访问后：
- 手机 Safari 里点"分享 → 添加到主屏幕"
- 电脑 Chrome/Edge 里点地址栏的"⬇ 安装"按钮
- 之后可以像原生 App 一样从桌面打开，独立窗口，甚至离线也能用（因为 Service Worker 已经缓存好所有文件）

---

## 八、这次做了什么（时间顺序）

按 `git log` 从早到晚：

1. **`f4957bb` 初始版本**：单文件的图片去水印工具，块匹配算法（不联网也能用，但效果一般）
2. **`b8419b9`、`b3c088f`** 优化去水印算法：改成相干块匹配 + 补丁复制
3. **`2b6f275`** 集成 LaMa AI 去水印，失败自动回退到本地算法；手机自动选轻量模型
4. **`9e79581`** 修复 GitHub Pages 上 wasm 多线程问题（无 COOP/COEP，强制单线程）
5. **`02df9ee`** 模型下载源改成 hf-mirror（HuggingFace 国内镜像），解决国内下载超时
6. **`90b1037`** 换成 MI-GAN 模型（28MB），放进仓库同源加载，彻底解决 CORS
7. **`b368f4a`** 修 AI 推理前先重绘一帧，避免"处理中"提示被主线程阻塞
8. **`649ae3b`** Service Worker 改成"网络优先"策略，避免用户永远吃到旧版本
9. **`84a8013`** 手机版双指缩放误画点：单指落笔延迟 90ms，期间第二指到达就判为缩放
10. **`da4d00d`** 手机版保存改用系统分享面板（能直接存到相册）
11. **`2011c17`** 清晰度调节：最高档用原图分辨率 1:1 处理，涂抹区完全不模糊
12. **`62dd0fc`** 电脑版加 PWA：可装到桌面，独立窗口 + 离线可用
13. **`9558fab`** 添加水印功能上线：模式切换器 + 文字水印（拖拽/旋转/描边/背景）
14. **`2de7703`** 水印大更新：多水印/水印库/模板/批量处理/图片编辑（画笔/裁剪/旋转），手机版同步
15. **`9837a7f`** AI 运行时本地化：ort.min.js 和 wasm 从 CDN 挪到 `vendor/ort/`，首次访问也无需联网
16. **`ec0ffcf`** 修 iOS Safari 里 wasmPaths 相对路径解析异常，改成绝对 URL

---

## 九、项目文件结构

```
ImageWR/
├── ImageWR.html          # 电脑版页面（1900+ 行，包含 HTML/CSS/JS）
├── mobile.html           # 手机版页面（1900+ 行，同样一体化）
├── index.html            # 最初的单文件版（老版本，向后兼容）
├── ai-inpaint.js         # AI 推理模块（onnxruntime + 模型加载）
├── sw.js                 # Service Worker：离线缓存策略
├── app.webmanifest       # 电脑版 PWA 清单
├── manifest.webmanifest  # 手机版 PWA 清单
├── serve.js              # 本地测试服务器（node serve.js 起）
├── gen-icons.js          # 生成 PWA 图标的小脚本
├── icon-192.png          # 应用图标（192×192）
├── icon-512.png          # 应用图标（512×512）
├── migan_pipeline_v2.onnx  # AI 模型文件（28 MB）
├── vendor/
│   └── ort/              # onnxruntime-web 运行时（本地化，11.5 MB）
│       ├── ort.min.js
│       ├── ort-wasm-simd-threaded.mjs
│       └── ort-wasm-simd-threaded.wasm
└── HOW_IT_WORKS.md       # 就是这个文件
```

---

## 十、一些技术细节（如果好奇）

### Canvas 的三层结构（去水印模式）
1. **workCanvas**（离屏）：存放"真正的图像像素"，AI 处理的对象
2. **maskCanvas**（离屏）：存放"红色涂抹"，纯粹的 mask
3. **view**（屏幕上看到的那个）：把前两个合成显示

编辑不动 workCanvas，只画 mask。点击"去除水印"时：把 workCanvas 和 mask 一起送进 AI，AI 返回结果后覆盖 workCanvas。这样能"撤销""重涂"。

### 加水印是非破坏的
水印不是"画到图片上"，而是**每次 render() 都在 workCanvas 上面叠加一次**。保存图片的时候才创建一个临时合成 Canvas 把水印烧进去导出 PNG。这样你可以随时拖拽、改颜色、删除，原图始终没变。

### 归一化坐标（水印库/模板能跨图用）
水印的位置和大小不存"像素值"（比如 `x=500, y=300, fontSize=48`），而是存**相对比例**（`nx=0.5, ny=0.3, nfontSize=0.03`）。这样把一张 4000×3000 图片上定制的水印保存起来，应用到一张 800×600 的新图上，位置和大小会自动等比例缩放到合理值。

### Service Worker 的缓存策略
- **HTML/JS/CSS**：网络优先 — 每次都去 GitHub 拉最新，拿不到才用缓存。避免用户永远吃到旧版本。
- **AI 模型/wasm**：缓存优先 — 一旦下载过就永远从本地拿，除非我们主动 bump 版本号（`CACHE = "dewatermark-v15"`）触发重下。

### 为什么手机上会遇到 iOS Safari 的坑
iOS Safari 对 ES Module 的动态 `import()` 有和其他浏览器不一样的路径解析规则。相对路径 `./vendor/ort/` 会被解析成相对于 `ort.min.js` 自身的位置，而不是 `document.baseURI`，导致路径重复变成 404。用 `new URL(path, location.href).href` 强制转成绝对 URL 就能绕开。

---

## 结语

这个项目从技术上来说没什么"高级魔法" —— 只用了浏览器本来就有的能力（Canvas + JS + Service Worker + WebAssembly）加上一个开源 AI 模型。真正花时间的是**把这些拼起来让它稳定跑在各种浏览器上**：Chrome、Safari、iOS、Android、桌面。每一次 commit 基本上都在解决一个新场景冒出来的兼容性问题。

Git/GitHub 的角色说白了就是"版本管理 + 免费的分发通道"：本地写代码 → commit 存快照 → push 到 GitHub → 别人打开链接就能用，链路很短。

有问题随时问我，或者直接看代码 —— 全部都是可读的源码，没有编译混淆。
